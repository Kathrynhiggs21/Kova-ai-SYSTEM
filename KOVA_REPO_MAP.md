# KOVA OS Repository Map (Compatibility Pointer)

This former map is retained only so older links do not break.

Use [`docs/architecture/KOVA_REPOSITORY_MAP.md`](docs/architecture/KOVA_REPOSITORY_MAP.md) as the single canonical repository map. Runtime enablement is defined only by `repositories[].enabled` in `kova_repos_config.json`, and module split policy is defined only by `config/core_modules.v1.json`.
